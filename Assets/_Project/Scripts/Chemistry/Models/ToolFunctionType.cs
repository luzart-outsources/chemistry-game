namespace ChemistryGame.Chemistry
{
    public enum ToolFunctionType
    {
        IndicatorPaper,    // Quỳ tím
        Burner,            // Đèn khò
        FilterPaper,       // Giấy lọc
        DistilledWater,    // Nước cất
        GasCollector,      // Bình thu khí + ống dẫn
        ReagentDropper,    // Thuốc thử (KSCN, HNO3)
        GasBubbler         // Sục khí (Cl2, O2)
    }
}
